# 湖南绿盾卫士检测技术有限公司 · 内部培训考核系统

> 依据《湖南省检验检测机构关键岗位人员考试大纲（2025年版）》开发的**纯前端内部培训考核系统**。
> 无需联网、无需数据库、无需服务器，双击 `index.html` 即可使用。

---

## 一、如何启动

**方式一（推荐，最简单）：** 直接双击 `index.html`，用浏览器（Chrome / Edge / 360 等 Chromium 内核浏览器）打开即可。

**方式二（本地服务器）：** 若双击打开后个别浏览器受限，可在本目录执行：

```bash
python -m http.server 8777
```

然后浏览器访问 `http://127.0.0.1:8777/index.html`。

> 系统优先使用 IndexedDB 存储，不可用时自动降级为 localStorage，再不可用时使用内存兜底——
> 因此无论是本地服务器还是 `file://` 直接打开都能正常运行。

---

## 二、系统结构

```
绿盾卫士培训考核系统/
├── index.html                  # 入口
├── assets/
│   ├── app.css                 # 样式
│   ├── store.js                # 存储层 + 题库仓库（两套题库严格分库）
│   ├── engine.js               # 组卷 / 计分引擎（严格按大纲 4.2）
│   ├── ui.js                   # 刷题 / 考试 / 成绩单 / 合格证明
│   ├── admin.js                # 后台管理
│   ├── main.js                 # 启动引导
│   └── xlsx.full.min.js        # Excel 解析库
├── data/
│   └── bank-keypost.js         # 内置关键岗位题库（9010 题，预生成）
└── tools/
    └── build_bank.py           # 从「关键岗位人员考试公示题库」重新生成数据
```

---

## 三、两大模块

首页提供两个独立入口，对应两套**完全隔离**的题库：

| 模块 | 用途 | 题库来源 |
|------|------|----------|
| **日常培训考核** | 公司内部各项培训的学习与考核 | **后台单独上传维护**，不与关键岗位题库混用 |
| **关键岗位人员考试** | 最高管理者 / 质量负责人 / 技术负责人 / 授权签字人 考前训练与模拟考 | 内置公示题库（桌面「关键岗位人员考试公示题库」） |

每个模块均有 **刷题模式**（自由练习、错题本、收藏）与 **考试模式**（模拟考、计时、防切屏、成绩单、合格证明）。

---

## 四、关键岗位人员考试（按大纲制作）

### 1. 组卷规则（大纲 4.2）

- **首次考试**（单选 1 分、多选 2 分、判断 2 分，满分 100，合格 70，时长 90 分钟）：

| 岗位 | 科目A（法/法规） | 科目B（质量/风险） | 科目C（通用技术） | 科目D（专业技术） |
|------|------|------|------|------|
| 最高管理者 | 单选25·多选15·判断10 | 单选5·多选5·判断5 | — | — |
| 质量负责人 | 单选10·多选5·判断5 | 单选15·多选10·判断5 | 单选5·多选5·判断5 | — |
| 技术负责人 | 单选5·多选5·判断2 | 单选5·多选5·判断3 | 单选12·多选6·判断6 | 单选8·多选4·判断4 |
| 授权签字人 | 单选5·多选5·判断2 | 单选5·多选5·判断3 | 单选8·多选4·判断4 | 单选12·多选6·判断6 |

- **报考岗位与兼任**：考试页提供「报考岗位」与「兼任其他关键岗位」两个下拉（最高管理者 / 技术负责人 / 质量负责人 / 授权签字人），系统按所选自动组卷；兼任时按兼任岗位要求考试（大纲 4.2.1.5）。
- **随机组题**：每场考试按上述配比从对应题库随机抽取，选项自动乱序，杜绝背题。

### 2. 科目D 范围（按你的要求）

内置题库**仅包含**：
- **2-生态环境监测类**（12 个二级类别）
- **7-卫生计生（产品）（疾控）类**（4 个二级类别）

其余科目D 大类未纳入。考试时科目D **每次只能选择一个专业大类**考试（符合大纲 4.2.2）。

### 3. 防切屏（考试纪律）

- 考试开始后自动监测窗口切换、最小化、切换标签页。
- 累计**切屏 3 次自动强制交卷**。
- 考试中禁用复制 / 右键（可在后台关闭）。
- 考试时间到系统自动提交答卷。

### 4. 合格证明

考试合格后可一键生成**考核合格证明**，支持打印 / 另存为 PDF。

---

## 五、日常培训考核

- 题库由管理员在**后台单独上传**，与关键岗位题库**物理隔离、互不可见、互不混用**。
- 在后台「日常考试方案」中配置考试方案（单选/多选/判断题量、分值、时长、合格线、选用题库）。
- 考试模式按方案随机组卷，同样具备切屏监测、计时、成绩单功能。

---

## 六、后台管理

1. 点击右上角「后台管理」进入。
2. **登录密码：`ldws2025`**（可在「系统设置」中修改）。
3. 后台功能：
   - **日常培训题库**：上传 / 删除日常培训题库（Excel）。
   - **日常考试方案**：新建 / 编辑考试方案。
   - **关键岗位题库**：查看内置公示题库情况（内置库不可删）。
   - **关键岗位参数**：配置首次/扩领域考试时长、合格线、切屏上限、选项乱序、防复制等。
   - **考试记录**：查看 / 导出全部考试记录（Excel）。
   - **系统设置**：备份 / 恢复、下载上传模板、清空数据、修改后台密码。

---

## 七、上传题库的 Excel 格式

表头需包含以下列（顺序不限，系统自动识别）：

| 列名 | 说明 |
|------|------|
| 题干 | 题目内容（必填） |
| 题型 | 单选题 / 多选题 / 判断题（可缺省，系统按答案与选项推断） |
| 答案 | 如 `A`、`AB`、`对`/`错`、`正确`/`错误` |
| 选项A ~ 选项E | 各选项内容 |
| 知识点 | （可选） |
| 解析 | （可选） |

系统自动去重、自动归一化判断题答案（对=A、错=B）、自动识别题型与选项。

> 后台「系统设置」中可下载标准上传模板。

---

## 八、数据与隐私

- 所有题库、考试记录、配置均保存在**本机浏览器**中，不上传任何服务器。
- 如需在多台电脑间迁移，请使用后台「系统设置 → 备份 / 恢复」。
- 注意：清除浏览器数据会同时清除本系统的题库与记录。

---

## 九、常见问题

- **双击打开后白屏？** 个别浏览器对 `file://` 限制较严，请改用本地服务器方式（见第一节），或换用 Chrome / Edge。
- **关键岗位考试里找不到某些科目D 类别？** 按需求仅内置了 2-生态环境监测类 与 7-卫生计生类，其余类别未纳入。
- **切屏被提前交卷？** 属正常防作弊行为；如确需宽松，可在后台「关键岗位参数」调高切屏上限或关闭监测。

---

## 十、公网分享（把考试发给任何人）

本系统已可发布为**公网静态网页**，任何人拿到链接、用任意浏览器打开即可考试，无需安装、无需登录。

### 1. 分享链接

- **推荐（可自定义命名、好记）· GitHub Pages 公网地址：**
  **`https://yunshenbuzhichu0719.github.io/lvdunweishi-kaoshi/`**
  仓库名 `lvdunweishi-kaoshi` 即为链接的一部分，可在 GitHub 仓库 Settings 里一键 Rename 改名（链接随之更新）。
- 备用镜像（云端静态空间，地址随机不可改名）：
  `https://f6ec6505f9eb4005820e565b3aedd845.gz5.agentos-app.net`
- 直接把上面的链接发给考生即可。考生打开后：
  - 首页两个入口：**日常培训考核** / **关键岗位人员考试**；
  - 进入对应模块 → 刷题模式 / 考试模式；
  - 关键岗位考试按大纲随机组卷，切屏 3 次自动交卷，合格可下载合格证明。

### 2. 直达考试链接（一键开考，体验更好）

可在分享链接后追加参数，让考生**点开就直奔某个具体考试**，只需填姓名即可开考：

| 想发哪种考试 | 链接后缀 | 说明 |
|------|------|------|
| 最高管理者·首次考试 | `?post=top&exam=1` | `exam=1` 首次（默认） |
| 质量负责人·首次考试 | `?post=quality&exam=1` | — |
| 技术负责人·首次考试 | `?post=tech&exam=1` | 技术负责人含科目D |
| 授权签字人·首次考试 | `?post=signer&exam=1` | 授权签字人含科目D |
| 日常培训·考试模式 | `?daily=exam` | 需后台已上传日常题库 |
| 日常培训·刷题模式 | `?daily=practice` | 需后台已上传日常题库 |

再追加 **`&kiosk=1`** 可隐藏右上角「后台管理」入口，让考生界面更干净。

> 示例（技术负责人·首次考试、隐藏后台）：
> `https://yunshenbuzhichu0719.github.io/lvdunweishi-kaoshi/?post=tech&exam=1&kiosk=1`

### 3. 成绩归集说明（重要）

当前版本是**纯前端静态页**，考生作答与成绩单都保存在**考生各自的浏览器本地**，
系统本身**不收集、不上传任何人的成绩**。因此：

- 考生当场可在自己屏幕上看到成绩、下载合格证明（PDF）；
- 组织者**需另加一个"成绩回传"功能**才能集中看到"谁考了、多少分"。

如果你需要集中阅卷/收集成绩，我可以再帮你加一个轻量回传（例如把成绩 POST 到一个表单/在线表格，
或在云端加一个最小后端做成绩收口）。告知我即可。

---

## 十一、集中收集成绩（组织者视角）

本系统为纯前端静态页，默认成绩只存考生本地。开启「成绩回传」后，考生交卷时成绩会**自动 POST 到你指定的收集地址**，你就能在一个后台看到"谁考了、多少分、是否合格"。

### 方式一：用随附的收集服务端（推荐自托管 / 内网友好）

项目内已提供 `tools/collector.js`（零依赖，仅需 Node.js）：

```bash
cd tools
PORT=3000 ADMIN_PW=你的查看密码 SECRET=你的密钥 node collector.js
```

- 查看成绩：浏览器打开 `http://<运行该服务的电脑IP>:3000/?pw=你的查看密码`
- 上报接口：`POST http://<该IP>:3000/api/score`（考试页会自动带上 `X-Report-Token: 你的密钥`）
- 支持**导出 CSV**、**清空成绩**；所有成绩保存在 `tools/scores.json`。

**最优部署（避免跨域/混合内容问题）**：让收集服务端**同时托管考试页**——

```bash
PORT=3000 STATIC=".." ADMIN_PW=xxx SECRET=xxx node collector.js
```

然后把 `http://<内网IP>:3000/` 发给大家。此时考试页与回传接口**同源**，无需处理跨域，公司内网直接用。

### 方式二：指向现有表单 / 问卷 Webhook

把"回传地址"填成**腾讯问卷 / 金数据 / 问卷星**等支持 Webhook 的 HTTPS 接口，考生成绩即进入对应在线表格。
注意：这类服务通常要求字段名匹配，接入时按其文档调整字段即可（回传载荷字段见 `tools/collector.js` 中的 `buildReportPayload`）。

### 在系统里开启回传

1. 后台「成绩回传」页：勾选"启用成绩回传"，填**回传地址**与**密钥**，保存。
2. 也可在分享链接后追加参数，让某次考试强制回传：

   | 参数 | 作用 |
   |------|------|
   | `?report=1` | 强制回传（即使全局未开启） |
   | `?reportUrl=<地址>` | 临时指定回传地址（适合不同考试发到不同收集表） |

   例：`https://.../?post=tech&exam=1&report=1&reportUrl=https://你的收集地址/api/score`

### 重要提醒（公网混合内容限制）

当前公网考试链接是 **HTTPS**（GitHub Pages 与 CloudStudio 均为 HTTPS）。若回传地址是 **HTTP**，浏览器会**拦截**该 POST（混合内容限制）。
因此：

- **公网收集**：回传地址必须是 **HTTPS**（方式二的问卷服务，或给方式一的服务套 HTTPS 反代 / 隧道）；
- **内网同源部署**（方式一的 `STATIC` 模式）：考试页与接口同为 http 同源，不受影响，最省心。

---

## 十二、考生登录与试卷存档

### 1. 考生登录（入口门禁）
打开网页先进入登录页，提供三种方式：

- **登录**：用「用户名 + 密码」登录（账号来自下方「注册」）。
- **注册**：填写用户名、密码、姓名（必填）、工号、部门，并设置**密保问题与答案**，提交后自动登录。账号保存在本机浏览器，下次可直接登录，且姓名 / 工号 / 部门自动带入考试。
- **忘记密码**：输入用户名 → 校验**密保答案** → 重置密码（无需邮箱 / 短信，纯本地验证）。

> 登录页下方的「登录 / 注册 / 忘记密码」切换标签位于**登录按钮下方**，登录 / 注册 / 忘记密码之间可随时点标签切换。已于 2026-07-31 移除「游客直接考试」入口。

> 说明：账号仅存于考生**本机浏览器**。这是纯前端静态应用，没有统一账号服务器；如需跨设备统一账号，请接入后端（参见第十节成绩回传的后端思路）。
> 密码以轻量哈希存储，非明文。

### 2. 进入模块后的返回导航
进入任意模块 / 刷题 / 考试 / 成绩单 / 合格证明 / 打印试卷后，**顶栏左侧均出现「‹ 返回」图标**，点击返回上一级界面（首页 → 模块 → 组卷 / 刷题 → 题目 / 成绩）。考试中点击返回会提示「放弃本场考试」，确认后返回。

### 3. 考试记录（后台）
进入「后台管理 → 考试记录」：
- 按**模块 / 姓名 / 工号 / 考试名称**筛选；
- 每条记录可「**查看**」（答卷解析）或「**打印**」（打印该考生完整试卷存档）；
- 「**打印记录表**」可把当前筛选出的全部记录打印 / 另存为 PDF 归档；
- 「导出 Excel」导出成绩明细，「清空记录」清空本机全部记录。
> 记录保存在考生各自浏览器本地；若需集中收集，参见第十节「成绩回传」。

### 4. 打印试卷存档
考生交卷后，在**成绩单页**点「**打印试卷存档**」即可生成带表头的完整试卷：考生信息、每题题干 / 选项、正确答案、考生作答、本题得分、解析，分页清晰，可直接打印或「另存为 PDF」作为考核存档。
（后台「考试记录」中单条「打印」、整表「打印记录表」同样走该打印样式。）
