# 绿盾卫士云版 · 新电脑一键启动说明

> 给换电脑、第一次拿到代码的人看。零基础也能照着做。

绿盾卫士培训考核系统（云版）自带 Node.js 后端，**不依赖任何第三方库**，一台装了 Node.js 的电脑就能跑起来。改代码用普通文本编辑器即可。

---

## 一、第一次用（新电脑，代码都还没有）

### 第 1 步：装 Node.js（必须）

1. 打开官网 <https://nodejs.org>
2. 点左边的绿色按钮 **LTS**（长期支持版），下载安装包
3. 一路点击「下一步」安装，**注意安装向导里要勾选 "Add to PATH"**（默认已勾选，别取消）

验证是否装好：

- 按 `Win + R`，输入 `cmd` 回车
- 在黑窗口里输入 `node -v`，能看到类似 `v18.19.1` 的版本号就成功了

### 第 2 步：装 Git（只需第一次拉代码，可选）

- 打开 <https://git-scm.com> 下载安装，一路下一步即可

### 第 3 步：拉代码

在 `cmd` 窗口里依次粘贴执行（每行回车）：

```bash
git clone https://github.com/yunshenbuzhichu0719/lvdunweishi-kaoshi.git
cd lvdunweishi-kaoshi
git checkout cloud
```

### 第 4 步：一键启动 🚀

- **Windows**：进入项目文件夹，**双击 `start.bat`**
- **Mac / Linux**：右键项目文件夹「在终端中打开」，执行 `bash start.sh`

### 第 5 步：打开浏览器

启动脚本窗口会提示访问地址，一般是：

```
http://localhost:3000
```

浏览器打开就能用了。**关掉那个脚本窗口，服务就停了。**

---

## 二、已经拉过代码（第二次及以后）

直接双击 `start.bat`（Windows）或执行 `bash start.sh`（Mac/Linux），然后开浏览器即可，不用再 clone。

---

## 三、默认管理员账号

- 用户名：`admin`
- 密码：`ldws2025`

首次运行会自动建库并生成这个账号。**登录后请尽快在后台修改默认密码。**

---

## 四、想自己改代码

用任意编辑器（VS Code、WorkBuddy 都行）打开项目文件夹。相关文件：

- 前端页面与逻辑：`public/`（HTML / JS）
- 后端服务与接口：`server.js`
- 部署到三丰云的脚本：`tools/_ssh_deploy.py`

改完保存，**纯前端改动刷新浏览器即可生效**；**改了 `server.js` 需要关掉脚本窗口重新双击启动。**

---

## 五、改完想更新到三丰云服务器

跟我说一声「**同步**」，我帮你把代码传上服务器，约 30 秒生效。
脚本已设为「只传代码、绝不覆盖服务器数据」，学员账号、考试记录、授权台账都不会丢。

---

## 六、常见问题

| 现象 | 解决办法 |
|------|----------|
| 提示「node 不是内部或外部命令」 | Node.js 没装好或没加进 PATH，重装并勾选 Add to PATH |
| 浏览器打不开 localhost:3000 | 确认启动脚本窗口还开着（关了就停了）；或端口被占用，改 `server.js` 第 16 行的 `PORT` 值 |
| 想保留线上学员数据 | 把服务器上的 `/opt/ldws/data-store.json` 复制到项目根目录，同名覆盖即可 |
| Mac/Linux 执行 start.sh 没权限 | 终端执行 `chmod +x start.sh` 后再 `bash start.sh` |

---

## 七、安全提醒

- ☑ 首次登录务必修改 `admin` 默认密码 `ldws2025`
- ☑ 本项目数据存在 `data-store.json`，**别把它提交到公开仓库**（已自动忽略）
- ☑ 生产环境建议通过 HTTPS 访问，避免账号密码在公网明文传输

---

> 服务器地址（已部署）：http://43.226.38.94/
> 仓库 cloud 分支：https://github.com/yunshenbuzhichu0719/lvdunweishi-kaoshi/tree/cloud
